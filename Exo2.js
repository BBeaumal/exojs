const scanf = require('scanf');

console.log('Please input an integer');
var numberEnd = scanf('%d');
const numberStart = 1;

for (i = 0; i < numberEnd; i++) {
    var somme = (numberEnd * (numberEnd + 1)) / 2;
}
console.log(somme);

